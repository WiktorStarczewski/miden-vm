# Number of eight-felt advice blocks in one aligned PVM OOD row. Pinned against the generated
# InputLayout by `pvm_ood_hook_matches_the_codegen_row_span`.
const OOD_ROW_DOUBLE_WORDS = 240

#! Processes one PVM row of out-of-domain evaluations.
#!
#! The row is the LMCS-aligned wire sequence used by the lifted PCS, in commitment-group order:
#!
#! - 16 preprocessed extension-field slots;
#! - 568 main extension-field slots across ten AIRs in proof order;
#! - 368 auxiliary-coordinate extension-field slots across ten AIRs in proof order;
#! - 8 quotient extension-field slots (four quadratic-extension chunks).
#!
#! This is 960 extension-field values = 1,920 felts = 240 `adv_pipe` blocks. Each block is stored,
#! folded into the DEEP fixed term with `horner_eval_ext`, and compressed into the Eidos
#! transcript.
#!
#! Inputs:  [scratch0, scratch1, cv, ptr, alpha_ptr, acc0, acc1]
#! Outputs: [scratch0, scratch1, cv', ptr, alpha_ptr, acc0', acc1']
pub proc process_row_ood_evaluations
    repeat.OOD_ROW_DOUBLE_WORDS
        adv_pipe
        horner_eval_ext
        bcompress
    end
end
